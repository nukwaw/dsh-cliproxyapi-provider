# Install the CLIProxyAPI provider plugin. It owns the CLIProxyAPI provider
# route itself (adapter, catalog sync, Fast mode, server-side web search).
- insert:
    - id: llm-cliproxyapi
      name: 'dsh-cliproxyapi-provider'
